import math
def checkArmstrongNumber(num):
    num=str(num)
    n=len(num)
    total=0
    for i num:
        total +=int(i)**n
        if int(num)==total:
            return 1
        else:
            return 0
n=input("Enter a Number:")
   if checksum(num):
       print("yes")
   else:
       print("No")
        
